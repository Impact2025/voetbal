# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env.local
.env*
!.env.example
.vercel
.env*.local

# Supabase CLI local cache — generated, never commit
supabase/.temp/

# Large binary assets — not for git
/trainingen/
homework-videos/raw/
cleanup-dupes.mjs

# Docs folder holds only Markdown — keep PDFs local (too large / non-source)
/docs/*.pdf
!/docs/*.md
/docs/*/\(1\).pdf

# Skills live under scripts/.skills/ — DO commit
!scripts/.skills/**
